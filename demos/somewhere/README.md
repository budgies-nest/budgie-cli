# Use another config and system file

```bash
budgie ask --prompt --rag \
--config ../../.budgie/budgie.config.json \
--system ../../.budgie/budgie.system.md \
--embeddings ../../.budgie/embeddings.json
```